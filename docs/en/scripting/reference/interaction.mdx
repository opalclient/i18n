# Interaction Proxies

These proxies handle player state, inventory, movement, rotation, and block/entity interaction.

---

## Player Proxy
**Global Binding:** `player`

Exposes local player state, position, health, combat utilities, and actions.

### Position

<Method name="getPosition" signature="Vec3d getPosition()" description="Returns the player's position in world space." returns={{ type: "Vec3d", desc: "Player position." }} />
<Method name="getEyePosition" signature="Vec3d getEyePosition()" description="Returns the player's eye-level position in world space." returns={{ type: "Vec3d", desc: "Eye position." }} />
<Method name="getBlockPosition" signature="BlockPos getBlockPosition()" description="Returns the floored block coordinates the player is standing in." returns={{ type: "BlockPos", desc: "Block position (ScriptBlockPos wrapper)." }} />
<Method name="getVelocity" signature="Vec3d getVelocity()" description="Returns the current velocity vector." returns={{ type: "Vec3d", desc: "Velocity." }} />
<Method name="getBoundingBox" signature="Box getBoundingBox()" description="Returns the player's axis-aligned bounding box." returns={{ type: "Box", desc: "Bounding box." }} />
<Method name="getStandingEyeHeight" signature="float getStandingEyeHeight()" description="Returns the eye height above feet." returns={{ type: "float", desc: "Eye height." }} />

### Rotation

<Method name="getYaw" signature="float getYaw()" description="Returns the current yaw in degrees." returns={{ type: "float", desc: "Yaw." }} />
<Method name="getPitch" signature="float getPitch()" description="Returns the current pitch in degrees." returns={{ type: "float", desc: "Pitch." }} />

### State

<Method name="isOnGround" signature="boolean isOnGround()" description="Whether the player is on the ground." returns={{ type: "boolean", desc: "True if grounded." }} />
<Method name="isInAir" signature="boolean isInAir()" description="Whether the player is airborne." returns={{ type: "boolean", desc: "True if in air." }} />
<Method name="getAirTicks" signature="int getAirTicks()" description="Ticks spent in the air continuously." returns={{ type: "int", desc: "Air ticks." }} />
<Method name="getGroundTicks" signature="int getGroundTicks()" description="Ticks spent on the ground continuously." returns={{ type: "int", desc: "Ground ticks." }} />
<Method name="isSneaking" signature="boolean isSneaking()" description="Whether the player is sneaking." returns={{ type: "boolean", desc: "True if sneaking." }} />
<Method name="isSprinting" signature="boolean isSprinting()" description="Whether the player is sprinting." returns={{ type: "boolean", desc: "True if sprinting." }} />
<Method name="isUsingItem" signature="boolean isUsingItem()" description="Whether the player is eating, blocking, drawing a bow, etc." returns={{ type: "boolean", desc: "True if using item." }} />
<Method name="getFallDistance" signature="double getFallDistance()" description="Current fall distance in blocks." returns={{ type: "double", desc: "Fall distance." }} />

### Health

<Method name="getHealth" signature="float getHealth()" description="Current health points." returns={{ type: "float", desc: "Health." }} />
<Method name="getMaxHealth" signature="float getMaxHealth()" description="Maximum health points." returns={{ type: "float", desc: "Max health." }} />
<Method name="getAbsorption" signature="float getAbsorption()" description="Absorption (golden hearts) amount." returns={{ type: "float", desc: "Absorption." }} />

### Combat

<Method name="canCrit" signature="boolean canCrit()" description="Whether the next attack will be a critical hit." returns={{ type: "boolean", desc: "True if can crit." }} />
<Method name="getAttackDamage" signature="double getAttackDamage()" description="Main-hand attack damage value." returns={{ type: "double", desc: "Damage." }} />
<Method name="getEntityInteractionRange" signature="double getEntityInteractionRange()" description="Maximum entity attack range (reach distance)." returns={{ type: "double", desc: "Range." }} />
<Method name="isHoldingWeapon" signature="boolean isHoldingWeapon()" description="Whether the player holds a sword, axe, or pickaxe." returns={{ type: "boolean", desc: "True if weapon." }} />
<Method name="getDistanceToEntity" signature="double getDistanceToEntity(Entity entity)" description="Box-to-box distance to the given entity." params={[{ name: "entity", type: "Entity", desc: "Target entity." }]} returns={{ type: "double", desc: "Distance." }} />
<Method name="getClosestPoint" signature="Vec3d getClosestPoint(Entity entity)" description="Closest bounding-box point on an entity from the player's eye." params={[{ name: "entity", type: "Entity", desc: "Target entity." }]} returns={{ type: "Vec3d", desc: "Closest point." }} />

### Bounding Box Checks

<Method name="isBoxEmpty" signature="boolean isBoxEmpty(double dx, double dy, double dz)" description="Whether an offset of the player's box has no collision." params={[{ name: "dx", type: "double", desc: "X offset." }, { name: "dy", type: "double", desc: "Y offset." }, { name: "dz", type: "double", desc: "Z offset." }]} returns={{ type: "boolean", desc: "True if empty." }} />
<Method name="isBoxEmptyBelow" signature="boolean isBoxEmptyBelow(double dy)" description="Whether the space below the player at the given offset is empty." params={[{ name: "dy", type: "double", desc: "Y offset (negative = below)." }]} returns={{ type: "boolean", desc: "True if empty." }} />

### Actions

<Method name="swingHand" signature="void swingHand(Hand hand)" description="Plays the hand swing animation." params={[{ name: "hand", type: "Hand", desc: "MAIN_HAND or OFF_HAND." }]} />
<Method name="useItem" signature="void useItem(Hand hand)" description="Right-click use + swing animation." params={[{ name: "hand", type: "Hand", desc: "MAIN_HAND or OFF_HAND." }]} />

---

## Inventory Proxy
**Global Binding:** `inventory`

Manages hotbar slots, item searching, and stack inspection.

### Slot Switching

<Method name="setSlot" signature="void setSlot(int slot)" description="Switches the player's selected hotbar slot normally (visible to client and server)." params={[{ name: "slot", type: "int", desc: "Hotbar index (0-8)." }]} />
<Method name="setSlotSilent" signature="void setSlotSilent(int slot)" description="Switches the slot server-side only. The client continues to render the original item." params={[{ name: "slot", type: "int", desc: "Hotbar index (0-8)." }]} />
<Method name="setSlotFullSilent" signature="void setSlotFullSilent(int slot)" description="Completely spoofs the slot switch packet. The server sees the change but client state is untouched." params={[{ name: "slot", type: "int", desc: "Hotbar index (0-8)." }]} />
<Method name="sendSlotPacket" signature="void sendSlotPacket(int slot)" description="Sends a raw slot change packet." params={[{ name: "slot", type: "int", desc: "Hotbar index (0-8)." }]} />
<Method name="getSelectedSlot" signature="int getSelectedSlot()" description="Returns the currently selected hotbar index." returns={{ type: "int", desc: "Slot index (0-8)." }} />

### Item Searching

<Method name="findBlock" signature="int findBlock()" description="Searches the hotbar for a valid placeable block." returns={{ type: "int", desc: "Slot index, or -1 if none found." }} />
<Method name="findItem" signature="int findItem(String name)" description="Searches the hotbar for an item containing the given name (case-insensitive substring)." params={[{ name: "name", type: "String", desc: "Substring to search for." }]} returns={{ type: "int", desc: "Slot index, or -1." }} />
<Method name="findItemInInventory" signature="int findItemInInventory(String name)" description="Searches the full inventory (0-35) for an item containing the name." params={[{ name: "name", type: "String", desc: "Substring to search for." }]} returns={{ type: "int", desc: "Slot index (0-35), or -1." }} />

### Stack Inspection

<Method name="getStack" signature="ItemStack getStack(int slot)" description="Returns the ItemStack in the specified hotbar slot." params={[{ name: "slot", type: "int", desc: "Hotbar index (0-8)." }]} returns={{ type: "ItemStack", desc: "Item stack." }} />
<Method name="getMainHandStack" signature="ItemStack getMainHandStack()" description="Returns the stack currently held in the main hand (accounts for silent switching)." returns={{ type: "ItemStack", desc: "Main hand stack." }} />
<Method name="getOffHandStack" signature="ItemStack getOffHandStack()" description="Returns the stack in the offhand." returns={{ type: "ItemStack", desc: "Offhand stack." }} />
<Method name="isHeldItemBlock" signature="boolean isHeldItemBlock()" description="Whether the main hand holds a placeable block." returns={{ type: "boolean", desc: "True if block." }} />
<Method name="isBlock" signature="boolean isBlock(int slot)" description="Whether the slot contains a placeable block." params={[{ name: "slot", type: "int", desc: "Slot index." }]} returns={{ type: "boolean", desc: "True if block." }} />
<Method name="getItemName" signature="String getItemName(int slot)" description="Returns the display name of the item in a slot." params={[{ name: "slot", type: "int", desc: "Slot index." }]} returns={{ type: "String", desc: "Item name." }} />
<Method name="getItemCount" signature="int getItemCount(int slot)" description="Returns the stack size in a slot." params={[{ name: "slot", type: "int", desc: "Slot index." }]} returns={{ type: "int", desc: "Count." }} />
<Method name="countItem" signature="int countItem(String name)" description="Total count of items matching the name across the entire inventory." params={[{ name: "name", type: "String", desc: "Item name substring." }]} returns={{ type: "int", desc: "Total count." }} />
<Method name="countBlocks" signature="int countBlocks()" description="Total valid placeable blocks in hotbar and offhand." returns={{ type: "int", desc: "Block count." }} />

---

## Movement Proxy
**Global Binding:** `movement`

Manipulates player velocity, direction, and state.

### Speed Queries

<Method name="getBlocksPerSecond" signature="double getBlocksPerSecond()" description="Horizontal speed in blocks/second." returns={{ type: "double", desc: "BPS." }} />
<Method name="getSpeed" signature="double getSpeed()" description="Horizontal speed in blocks/tick." returns={{ type: "double", desc: "BPT." }} />
<Method name="isMoving" signature="boolean isMoving()" description="Whether the player is pressing any movement keys." returns={{ type: "boolean", desc: "True if moving." }} />

### Speed Manipulation

<Method name="setSpeed" signature="void setSpeed(double speed)" description="Sets the player's horizontal speed in the current movement direction." params={[{ name: "speed", type: "double", desc: "Speed in blocks/tick." }]} />
<Method name="setSpeed (strafe)" signature="void setSpeed(double speed, double strafePercentage)" description="Sets speed with a blend between forward and strafe motion." params={[{ name: "speed", type: "double", desc: "Speed in blocks/tick." }, { name: "strafePercentage", type: "double", desc: "0.0 (forward) to 1.0 (strafe)." }]} />
<Method name="setSpeed (directional)" signature="void setSpeed(double speed, float yaw)" description="Sets speed along a specific yaw angle." params={[{ name: "speed", type: "double", desc: "Speed." }, { name: "yaw", type: "float", desc: "Direction angle." }]} />
<Method name="setEntitySpeed" signature="void setEntitySpeed(Entity entity, double speed, double yaw)" description="Sets the velocity of an arbitrary entity." params={[{ name: "entity", type: "Entity", desc: "Target entity." }, { name: "speed", type: "double", desc: "Speed." }, { name: "yaw", type: "double", desc: "Direction." }]} />
<Method name="getSwiftnessSpeed" signature="double getSwiftnessSpeed(double speed)" description="Applies Speed potion effect bonus to a base speed." params={[{ name: "speed", type: "double", desc: "Base speed." }]} returns={{ type: "double", desc: "Modified speed." }} />

### Direction

<Method name="getMoveYaw" signature="float getMoveYaw()" description="Movement yaw based on WASD input and camera rotation." returns={{ type: "float", desc: "Yaw." }} />
<Method name="getDirectionDegrees" signature="float getDirectionDegrees()" description="Movement direction in degrees." returns={{ type: "float", desc: "Degrees." }} />
<Method name="getDirectionRadians" signature="double getDirectionRadians()" description="Movement direction in radians." returns={{ type: "double", desc: "Radians." }} />
<Method name="yawPos" signature="double[] yawPos(float yaw, double value)" description="Returns [deltaX, deltaZ] offset for the given yaw and distance." params={[{ name: "yaw", type: "float", desc: "Direction." }, { name: "value", type: "double", desc: "Distance." }]} returns={{ type: "double[]", desc: "[deltaX, deltaZ]" }} />

---

## Rotation Proxy
**Global Binding:** `rotation`

Server-side rotation submission and angle math.

### Submission

<Method name="set" signature="void set(float yaw, float pitch)" description="Submits an instant rotation packet." params={[{ name: "yaw", type: "float", desc: "Target yaw." }, { name: "pitch", type: "float", desc: "Target pitch." }]} />
<Method name="setSmooth" signature="void setSmooth(float yaw, float pitch, float speed)" description="Submits a smooth rotation limited to max degrees per tick." params={[{ name: "yaw", type: "float", desc: "Target yaw." }, { name: "pitch", type: "float", desc: "Target pitch." }, { name: "speed", type: "float", desc: "Max degrees/tick." }]} />

### Calculation

<Method name="getRotationFromPosition" signature="Vec2f getRotationFromPosition(Vec3d pos)" description="Calculates yaw and pitch to face a world position." params={[{ name: "pos", type: "Vec3d", desc: "Target position." }]} returns={{ type: "Vec2f", desc: "Yaw/pitch pair." }} />
<Method name="getRotationFromBlock" signature="Vec2f getRotationFromBlock(BlockPos pos, Direction face)" description="Calculates rotation to face the center of a block face." params={[{ name: "pos", type: "BlockPos", desc: "Block." }, { name: "face", type: "Direction", desc: "Face to aim at." }]} returns={{ type: "Vec2f", desc: "Yaw/pitch." }} />
<Method name="getRotationVector" signature="Vec3d getRotationVector(float pitch, float yaw)" description="Returns the unit look vector for given angles." params={[{ name: "pitch", type: "float", desc: "Pitch." }, { name: "yaw", type: "float", desc: "Yaw." }]} returns={{ type: "Vec3d", desc: "Direction vector." }} />

### Raycasting

<Method name="getRotationFromRaycastedBlock" signature="RaytracedRotation getRotationFromRaycastedBlock(BlockPos pos, Direction side, Vec2f priorityRotations, Vec3d playerPos)" description="Validates if a rotation can hit a specific block face via raycast. Returns null if the block face isn't reachable." params={[{ name: "pos", type: "BlockPos", desc: "Target block." }, { name: "side", type: "Direction", desc: "Target face." }, { name: "priorityRotations", type: "Vec2f", desc: "Preferred rotation to start from." }, { name: "playerPos", type: "Vec3d", desc: "Player eye position." }]} returns={{ type: "RaytracedRotation", desc: "Result with yaw, pitch, and hitResult. Null if unreachable." }} />

<Method name="getRotationFromRaycastedEntity" signature="RaytracedRotation getRotationFromRaycastedEntity(Entity entity, Vec3d closestVec, double range)" description="Validates rotation to hit an entity via raycast." params={[{ name: "entity", type: "Entity", desc: "Target." }, { name: "closestVec", type: "Vec3d", desc: "Closest point on entity bounding box." }, { name: "range", type: "double", desc: "Max range." }]} returns={{ type: "RaytracedRotation", desc: "Result or null." }} />

### Queries

<Method name="getRotation" signature="Vec2f getRotation()" description="Returns the current server-side rotation." returns={{ type: "Vec2f", desc: "Yaw/pitch." }} />
<Method name="getRotationDifference" signature="float getRotationDifference(Vec2f a, Vec2f b)" description="Angular difference between two rotations." params={[{ name: "a", type: "Vec2f", desc: "First." }, { name: "b", type: "Vec2f", desc: "Second." }]} returns={{ type: "float", desc: "Degrees." }} />
<Method name="getEntityFOV" signature="float getEntityFOV(Entity entity)" description="Degrees from look direction to entity." params={[{ name: "entity", type: "Entity", desc: "Target." }]} returns={{ type: "float", desc: "Degrees." }} />
<Method name="isEntityInFOV" signature="boolean isEntityInFOV(Entity entity, float fov)" description="Whether entity is within a FOV cone." params={[{ name: "entity", type: "Entity", desc: "Target." }, { name: "fov", type: "float", desc: "Half-angle in degrees." }]} returns={{ type: "boolean", desc: "True if in FOV." }} />

### Anti-Cheat Helpers

| Method | Description |
|---|---|
| `patchConstantRotation(rotation, prevRotation)` | Add jitter to avoid constant-rotation detection |
| `getSensitivityModifiedRotation(original)` | Snap to Minecraft's sensitivity grid |
| `getSentRotation(original)` | Full sensitivity + vanilla transform |
| `getDuplicateWrapped(value, target)` | Avoid duplicate rotation packet detection |

---

## InteractionManager
**Access via:** `mc.interactionManager`

Wraps `ClientPlayerInteractionManager`. The local player is injected automatically.

### Block Interaction

<Method name="interactBlock" signature="void interactBlock(Hand hand, HitResult hitResult)" description="Right-clicks a block face. hitResult comes from RaytracedRotation.getHitResult()." params={[{ name: "hand", type: "Hand", desc: "MAIN_HAND or OFF_HAND." }, { name: "hitResult", type: "HitResult", desc: "From raytrace." }]} />
<Method name="updateBlockBreakingProgress" signature="boolean updateBlockBreakingProgress(BlockPos pos, Direction dir)" description="Advances block-breaking progress on the given face." params={[{ name: "pos", type: "BlockPos", desc: "Block." }, { name: "dir", type: "Direction", desc: "Face." }]} returns={{ type: "boolean", desc: "True if progress updated." }} />
<Method name="cancelBlockBreaking" signature="void cancelBlockBreaking()" description="Cancels any in-progress block break." />
<Method name="isBreakingBlock" signature="boolean isBreakingBlock()" description="Whether a block is currently being broken." returns={{ type: "boolean", desc: "True if breaking." }} />

### Entity Interaction

<Method name="attackEntity" signature="void attackEntity(Entity entity)" description="Attacks the given entity with the main hand." params={[{ name: "entity", type: "Entity", desc: "Target." }]} />
<Method name="interactItem" signature="void interactItem(Hand hand)" description="Right-click use without targeting (throw pearl, eat, etc.)." params={[{ name: "hand", type: "Hand", desc: "MAIN_HAND or OFF_HAND." }]} />
<Method name="stopUsingItem" signature="void stopUsingItem()" description="Stops current item use (release bow, stop eating)." />

### Example — Scaffold Pattern

```javascript
const below = new BlockPos(
    player.getBlockPosition().getX(),
    player.getBlockPosition().getY() - 1,
    player.getBlockPosition().getZ()
);

if (!world.isReplaceable(below)) return;

const dirs = world.getAdjacentDirections(below);
if (dirs.isEmpty()) return;

const side = dirs.get(0);
const neighbor = below.offset(side);

const raytrace = rotation.getRotationFromRaycastedBlock(
    neighbor,
    side.getOpposite(),
    rotation.getRotation(),
    player.getEyePosition()
);
if (raytrace === null) return;

rotation.set(raytrace.getYaw(), raytrace.getPitch());
mc.interactionManager.interactBlock(MAIN_HAND, raytrace.getHitResult());
player.swingHand(MAIN_HAND);
```
